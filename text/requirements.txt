# ── core data & analysis ───────────────────────────────────────────────────────
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26
requests>=2.31

# ── dashboard ──────────────────────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.22

# ── Excel output ───────────────────────────────────────────────────────────────
openpyxl>=3.1
xlsxwriter>=3.1

# ── ML model (projection layer) ───────────────────────────────────────────────
lightgbm>=4.3
scikit-learn>=1.4
joblib>=1.3
optuna>=3.6
pyarrow>=14.0

# ── Dolt training cache (optional — pip install mysql-connector-python) ───────
# mysql-connector-python>=8.3

# ── news sentiment — FinBERT (downloads ~440 MB model on first use) ───────────
transformers>=4.40
torch>=2.2           # CPU-only is fine: pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── news sentiment — Claude API (optional, set ANTHROPIC_API_KEY) ─────────────
anthropic>=0.27
